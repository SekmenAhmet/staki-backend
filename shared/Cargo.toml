[package]
name = "shared"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.8.6"
serde = { version = "1", features = ["derive"] }
once_cell = "1.19"
jsonwebtoken = { version = "10.1.0", features = ["rust_crypto"] }
chrono = "0.4"
